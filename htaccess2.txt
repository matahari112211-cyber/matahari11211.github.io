# public_html/.htaccess — BRIDGE ke telupress/public
RewriteEngine On

# 1) Force HTTPS (letakkan paling atas biar tidak double redirect)
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# 2) Matikan LSCache supaya tidak bentrok
<IfModule LiteSpeed>
RewriteRule .* - [E=cache-control:no-cache]
</IfModule>

# 3) Lindungi direktori sensitif Laravel di bawah /telupress
RewriteRule ^telupress/(vendor|storage|bootstrap|config|resources|routes)/ - [F,L]

# 4) Arahkan SEMUA request ke /telupress/public
RewriteCond %{REQUEST_URI} !^/telupress/public/
RewriteRule ^(.*)$ /telupress/public/$1 [L,QSA]
